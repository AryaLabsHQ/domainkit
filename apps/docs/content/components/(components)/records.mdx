---
title: Records
description: Show your DNS records in a table, or let users copy and download a zone file for setup screens, support, or export.
---

The records namespace shows the DNS requirements from your app and any evidence you pass to it. It
does not read DNS or decide whether the product is ready.

<ReactExample story="records" />

## Installation

Install the packaged React components:

```package-install
@domainkit/react domainkit effect @effect/atom-react react react-dom
```

For standalone, copied display components, install the matching registry items:

```package-install
npx shadcn@latest add https://domain-kit.dev/r/dns-table.json https://domain-kit.dev/r/copy-value.json https://domain-kit.dev/r/dns-status.json
```

The registry components accept plain props and do not include DomainKit transport, planning, or
observation models.

## Table

`Records.Table` renders type, name, value, priority, and any evidence you pass in. Evidence is
grouped by `recordId`, so provider and public-DNS results can stay separate.

```tsx
<Records.Table records={records} evidence={observation.publicDns} />
```

## Zone file

`Records.ZoneFile` turns the same records into a zone file for copy and download actions. Clipboard
and download behavior starts only after a user action, so the component can render on the server.

```tsx
<Records.ZoneFile domain={domain} records={records} />
```

## Accessibility

Copy and download controls have action-specific accessible labels. Preserve those labels when
supplying custom icons. Keep the table header cells and visible status text when composing lower
level record parts.

## API reference

### `Records.Table`

<TypeTable
  type={{
    copiedIcon: {
      type: "ReactNode",
      description: "Optional icon shown after a value has been copied.",
    },
    copyIcon: {
      type: "ReactNode",
      description: "Optional icon shown for a copy action.",
    },
    evidence: {
      type: "ReadonlyArray<Transport.ObservationEvidence>",
      description: "Optional provider or public-DNS evidence grouped by record ID.",
    },
    records: {
      type: "ReadonlyArray<Transport.DnsRecord>",
      required: true,
      description: "DNS records to render as table rows.",
    },
  }}
/>

### `Records.ZoneFile`

<TypeTable
  type={{
    copiedIcon: {
      type: "ReactNode",
      description: "Optional icon shown after the zone file has been copied.",
    },
    copiedLabel: {
      type: "string",
      default: '"Copied"',
      description: "Label shown after copying the zone file.",
    },
    copyIcon: {
      type: "ReactNode",
      description: "Optional icon shown for the copy action.",
    },
    copyLabel: {
      type: "string",
      default: '"Copy zone"',
      description: "Label for the copy action.",
    },
    domain: {
      type: "string",
      required: true,
      description: "Domain used in the downloaded zone file name.",
    },
    downloadIcon: {
      type: "ReactNode",
      description: "Optional icon shown for the download action.",
    },
    downloadLabel: {
      type: "string",
      default: '"Download"',
      description: "Label for the download action.",
    },
    records: {
      type: "ReadonlyArray<Transport.DnsRecord>",
      required: true,
      description: "DNS records written to the zone file.",
    },
  }}
/>

### `Records.CopyValue`

<TypeTable
  type={{
    copiedIcon: {
      type: "ReactNode",
      description: "Optional icon shown after the value has been copied.",
    },
    copiedLabel: {
      type: "string",
      default: '"Copied"',
      description: "Action label shown after copying.",
    },
    copyIcon: {
      type: "ReactNode",
      description: "Optional icon shown for the copy action.",
    },
    copyLabel: {
      type: "string",
      default: '"Copy"',
      description: "Action label shown before copying.",
    },
    value: {
      type: "string",
      required: true,
      description: "Text shown and copied after the user activates the action.",
    },
  }}
/>

Read [Observe DNS](/docs/guides/observe-dns) for the difference between provider readback and public
DNS propagation.
