---
title: Verification
description: Show provider checks and public DNS for each record without hiding propagation or product readiness from the person setting it up.
---

`Verification.Status` requests fresh checks through the host transport and shows provider readback
separately from public DNS.

<ReactExample story="verification" />

## Installation

```package-install
@domainkit/react domainkit effect @effect/atom-react react react-dom
```

For standalone status and async-state primitives, use the registry:

```package-install
npx shadcn@latest add https://domain-kit.dev/r/dns-status.json https://domain-kit.dev/r/async-state.json
```

Those copied components only render what you pass to them. They do not call the DomainKit transport
or interpret observation evidence.

## Observation states

| State         | Meaning                                                 |
| ------------- | ------------------------------------------------------- |
| `Idle`        | No check has been requested in this controller.         |
| `Observing`   | The host is reading provider and public-DNS evidence.   |
| `Observation` | Evidence is available for the requested records.        |
| `Failed`      | The check failed; previous evidence may still be shown. |

`Found`, `Missing`, `Mismatch`, and provider failures describe individual records. Your app decides
whether that evidence makes the product feature ready.

## Usage

Include the connected provider context when available. Without it, the host can still perform public
DNS observation according to its transport implementation.

```tsx
<Verification.Status config={{ connection, domain, records }} />
```

## Accessibility

The observe action exposes its busy state and prevents duplicate requests. Keep evidence notes and
failure messages visible; color alone cannot distinguish missing, mismatched, or unavailable DNS.

## API reference

### `Verification.Status`

<TypeTable
  type={{
    config: {
      type: "Verification.ObserveConfig",
      required: true,
      description: "Inputs used by the status controller.",
    },
  }}
/>

### `Verification.ObserveConfig`

<TypeTable
  type={{
    connection: {
      type: "Transport.Connected",
      description: "Optional connected attachment used for provider checks.",
    },
    domain: {
      type: "string",
      required: true,
      description: "Customer domain whose records are checked.",
    },
    records: {
      type: "ReadonlyArray<Transport.DnsRecord>",
      required: true,
      description: "DNS records to compare with provider and public-DNS evidence.",
    },
    sources: {
      type: "{ provider?: boolean; publicDns?: boolean }",
      description:
        "Select the checks to run; provider follows connection presence and public DNS is on by default.",
    },
  }}
/>

### `Verification.ObserveAction`

<TypeTable
  type={{
    controller: {
      type: "Verification.Controller",
      required: true,
      description: "Controller whose observe command the action runs.",
    },
  }}
/>

Read [Provider evidence and public DNS](/docs/core/verification) for the observation model.
