---
title: Connection
description: Connect a customer domain to a supported DNS provider through your app's server while keeping credentials and callback routes private.
---

`Connection.Flow` renders provider discovery, authorization choices, retry states, and attachment
through your app's `Transport`. Provider credentials never enter the browser.

<ReactExample story="connection" />

## Installation

```package-install
@domainkit/react domainkit effect @effect/atom-react react react-dom
```

Import the default styles once in your application entry point:

```tsx
import "@domainkit/react/styles.css";
```

## Usage

Render the flow inside the `DomainKit.Root` that provides your transport, theme, messages, and
provider marks.

```tsx
import { Connection, DomainKit } from "@domainkit/react";

<DomainKit.Root transport={transportLayer}>
  <Connection.Flow domain="mail.example.com" />
</DomainKit.Root>;
```

The transport endpoint must authenticate the user, check tenant policy, keep credentials on the
server, and save connection and domain-attachment state. See [Transport](/docs/react/transport) for
the request boundary.

## States

| State          | Meaning                                                                 |
| -------------- | ----------------------------------------------------------------------- |
| `Loading`      | The flow is detecting the provider or restoring an existing connection. |
| `Disconnected` | A provider can be selected or an existing authorization can be reused.  |
| `Submitting`   | The host transport is processing a token or attachment request.         |
| `Redirecting`  | The browser is leaving for provider authorization.                      |
| `Connected`    | The host returned an active connection and domain attachment.           |
| `Detaching`    | A disconnect request is in progress.                                    |
| `Unsupported`  | The host cannot offer an automatic connection for this domain.          |
| `Failure`      | The operation failed with an explicit retry policy.                     |

## Composition

Use `Connection.useController(domain)` when the packaged flow does not match your product screen.
Compose `Root`, `Status`, `ConnectTrigger`, `Dialog`, and `RetryAction` around the same controller.
The [Host connection](/components/host-connection) page shows that integration level.

## Accessibility

`Connection.Dialog` uses dialog semantics and returns focus to its trigger. Keep a visible trigger
label that names the provider, and do not suppress failure text when applying custom styles.

## API reference

### `Connection.Flow`

<TypeTable
  type={{
    domain: {
      type: "string",
      required: true,
      description: "Domain whose provider connection the flow manages.",
    },
  }}
/>

### `Connection.Dialog`

<TypeTable
  type={{
    controller: {
      type: "Connection.Controller",
      required: true,
      description: "Controller that stores connection state and runs its commands.",
    },
    snapshot: {
      type: "Disconnected",
      required: true,
      description: "Disconnected provider data used to show authorization options.",
    },
  }}
/>

### `Connection.DisconnectDialog`

<TypeTable
  type={{
    connection: {
      type: "Transport.Connected",
      required: true,
      description: "Connected domain attachment to disconnect.",
    },
    controller: {
      type: "Connection.Controller",
      required: true,
      description: "Controller that runs the detach operation.",
    },
    onOpenChange: {
      type: "(open: boolean) => void",
      description: "Called when the dialog opens or closes.",
    },
    open: {
      type: "boolean",
      description:
        "Set this to control the dialog; omit it to let the dialog manage its own state.",
    },
    trigger: {
      type: "ReactElement | null",
      description: "Optional trigger element. Pass null to render no trigger.",
    },
  }}
/>

For the complete namespace inventory and shared runtime contracts, see the
[React API reference](/docs/reference/react).
