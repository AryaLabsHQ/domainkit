---
title: Host lifecycle
description: Put connection, review, apply, cleanup, and disconnect actions in your domain screen while your app keeps identity, storage, and policy.
---

This level keeps DomainKit's controllers and dialogs while your app owns the outer row and decides
when each action is available.

<ReactExample story="host-lifecycle" />

## Installation

```package-install
@domainkit/react domainkit effect @effect/atom-react react react-dom
```

## Lifecycle ownership

Provisioning and cleanup are separate reviewed operations:

1. `Provisioning.Flow` requests an exact `create` / `noop` / `conflict` plan.
2. The user authorizes that plan’s digest before the host applies it.
3. The host persists the returned receipt, including partial results.
4. `Cleanup.Flow` plans deletion from that receipt and requests separate authorization.

Do not treat disconnecting a provider as cleanup. Your product decides whether cleanup is needed,
whether a receipt is still valid, and whether a user may remove records.

## Product readiness

A completed provider write does not prove that public DNS has propagated. Use
[Verification](/components/verification) and combine its provider/public-DNS observations with your
own product readiness policy.

## Accessibility

Keep destructive cleanup and disconnect actions distinct in text, not color alone. Preserve the
confirmation dialogs, loading states, and failure outcomes supplied by the parts.

## API reference

### `Provisioning.Flow`

<TypeTable
  type={{
    connection: {
      type: "Transport.Connected",
      required: true,
      description: "Connected domain attachment used to plan and apply records.",
    },
    onApplied: {
      type: "(result: Applied | Partial) => void",
      description: "Called after the host returns a complete or partial apply result.",
    },
    records: {
      type: "ReadonlyArray<Transport.DnsRecord>",
      required: true,
      description: "DNS requirements included in the plan.",
    },
    showRecords: {
      type: "boolean",
      default: "true",
      description: "Whether the flow shows the records table beside its review action.",
    },
    trigger: {
      type: "ReactElement",
      description: "Optional element used as the review trigger.",
    },
  }}
/>

### `Cleanup.Flow`

<TypeTable
  type={{
    connection: {
      type: "Transport.Connected",
      required: true,
      description: "Connected domain attachment whose applied records may be cleaned up.",
    },
    onCleaned: {
      type: "(result: Cleanup.Completion) => void",
      description: "Called after the host returns a complete or partial cleanup result.",
    },
    receiptId: {
      type: "string",
      required: true,
      description: "Apply receipt that lists the records eligible for cleanup.",
    },
    trigger: {
      type: "ReactElement",
      description: "Optional element used as the cleanup review trigger.",
    },
  }}
/>

### `Connection.DisconnectDialog`

<TypeTable
  type={{
    connection: {
      type: "Transport.Connected",
      required: true,
      description: "Connected domain attachment to disconnect.",
    },
    controller: {
      type: "Connection.Controller",
      required: true,
      description: "Controller that runs the detach operation.",
    },
    onOpenChange: {
      type: "(open: boolean) => void",
      description: "Called when the dialog opens or closes.",
    },
    open: {
      type: "boolean",
      description: "Set this to control the dialog; omit it to use its own state.",
    },
    trigger: {
      type: "ReactElement | null",
      description: "Optional trigger element. Pass null to render no trigger.",
    },
  }}
/>

Read [Plans, authorization, and receipts](/docs/core/plans) for the lifecycle invariants behind
these controls.
