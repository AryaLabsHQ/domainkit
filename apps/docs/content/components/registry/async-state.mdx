---
title: Async state
description: Show loading, empty, and error states with simple children while your app owns fetching, retries, and the message shown to users.
---

`async-state` contains three display components for common async states. They do not fetch data,
retry requests, or own a transport.

<Component path="registry/async-state" />

## Installation

```package-install
npx shadcn@latest add https://domain-kit.dev/r/async-state.json
```

## Usage

```tsx
<LoadingState>Checking DNS…</LoadingState>
<EmptyState>No domains connected.</EmptyState>
<ErrorState>Could not load domains. Try again.</ErrorState>
```

Each component also has a useful default message when no children are supplied.

## API

| Component      | Props                  | Default message         |
| -------------- | ---------------------- | ----------------------- |
| `LoadingState` | `children?: ReactNode` | `Loading…`              |
| `EmptyState`   | `children?: ReactNode` | `Nothing here yet.`     |
| `ErrorState`   | `children?: ReactNode` | `Something went wrong.` |

All three components also accept the native props and `className` supported by the shadcn `Empty`
root.

Keep retry actions, error details, and loading ownership in your app. If an error is related to
provider authorization or DNS checks, show that context with the relevant packaged flow instead of
using this component to run the lifecycle.
