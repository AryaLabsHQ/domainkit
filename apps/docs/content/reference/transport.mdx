---
title: domainkit/client
description: The browser transport over the routes you mounted, its optional capability groups, the connect methods it sends, and the Promise adapters.
sidebar:
  label: domainkit/client
seo:
  title: DomainKit client transport reference
---

```ts
import { Transport } from "domainkit/client";
```

{/* reference-inventory:start */}

| Namespace   | Carries                                                                                       |
| ----------- | --------------------------------------------------------------------------------------------- |
| `Transport` | `Interface`, `fromFetch`, `fromAsync`, `toAsync`, `capabilities`, `allCapabilities`, `Method` |

{/* reference-inventory:end */}

## Capability groups

Every group on `Transport.Interface` is optional, so a host that mounts part of the route group gets
a transport that typechecks and a UI that renders only what the server can serve.

| Group          | Methods                                                          |
| -------------- | ---------------------------------------------------------------- |
| `connection`   | `inspect`, `discover`, `start`, `attach`, `detach`, `disconnect` |
| `provisioning` | `plan`, `approve`, `reject`, `apply`, `attempt`                  |
| `verification` | `observe`                                                        |
| `cleanup`      | `plan`, `approve`, `reject`, `apply`                             |

`verification.observe(domain, options?)` takes an optional `requirements` array. Supplied
requirements win; without them the server uses the attachment's latest provisioning receipt, and a
domain with neither answers `InvalidInput`.

<Snippet file="examples/client/transport.ts" region="capabilities" title="Declaring capabilities" />

## Building one

<Snippet file="examples/client/transport.ts" region="create" title="Over the mounted routes" />

<Snippet file="examples/client/transport.ts" region="headers" title="With host headers" />

`headers` is either a `HeadersInit` or a function returning one, awaited per request, so a token
your app refreshes is read at call time.

## Calling it

Every method returns `Effect<A, DomainKit.Error>` with no requirements.

<Snippet file="examples/client/transport.ts" region="call" title="From Effect" />

<Snippet file="examples/client/transport.ts" region="observe" title="Observing a requirement set" />

<Snippet file="examples/client/transport.ts" region="promises" title="From Promises" />

## Connect methods

`Transport.Method` mirrors `Connect.Method` over the wire, so a client never writes a tag literal.

| Constructor                        | Sends                                                    |
| ---------------------------------- | -------------------------------------------------------- |
| `Method.token(values)`             | A lone string, or the values the method descriptor names |
| `Method.oauth({ returnTo })`       | An OAuth start that answers with a redirect              |
| `Method.integration({ returnTo })` | A marketplace install that answers with a redirect       |

## Failures

A failure arrives as the same `DomainKit.Error` the lifecycle raised, reason intact. A reply the
transport cannot read as one is classified from its status, naming the transport's base URL as the
origin.

| Status        | Becomes                                                                  |
| ------------- | ------------------------------------------------------------------------ |
| 401           | `Unauthenticated`                                                        |
| 403           | `Forbidden`                                                              |
| 409           | `ProviderConflict`                                                       |
| 429 and 5xx   | `ProviderUnavailable`, retryable, with `retryAfterMs` from `retry-after` |
| anything else | `ProviderRejected`, carrying the status as its code                      |
